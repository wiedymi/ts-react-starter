import React, { ReactNode } from 'react'
import { Wrapper } from './styles'

type Props = {
  children: ReactNode
}

const Footer = (props: Props) => <Wrapper>Footer context</Wrapper>

export default Footer
